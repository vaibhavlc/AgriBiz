# Frontend Environment Configuration Example (Vercel)

# URL of the deployed Backend Service on Render (without trailing slash or API subpath)
VITE_API_URL=https://your-backend-service.onrender.com

# URL of the Socket.IO Realtime Gateway on Render (without trailing slash)
VITE_SOCKET_URL=https://your-backend-service.onrender.com
